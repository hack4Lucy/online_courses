// Even/Odd selectors using the TABLE class
$(document).ready(function() {
  // $('.tbl_first tr:even').addClass('highlight');   // <.table tr> is CLASS selector
  // $('.tbl_first tr:odd').addClass('highlight');

  // for DIV
  $('.area p:even').addClass('highlight');   // <.area p> is CLASS selector
});
